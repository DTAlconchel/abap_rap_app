projection;
strict ( 2 );
use draft;

define behavior for ZC_TEST_RAP alias Test
use etag

{
  use create;
  use update;
  use delete;

  use action Approve;
  use action Reject;

  // Exponer la composición en la proyección
  use association _Items { create; with draft; }     // <-- CAMBIO

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}

define behavior for ZC_TEST_RAP_ITM alias Item
{
  use update;
  use delete;	

  use association _Test;
}